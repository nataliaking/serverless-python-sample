version: 2
jobs:
   build:
     docker:
       - image: circleci/python:2.7
     steps:
       - checkout
       - run:
          name: Install PyTest
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install pytest
       - run: echo "hello world"
       - run:
          name: PyTest run
          command: python py.test